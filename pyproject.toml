[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "CMOR-438"
version = "1.0.0"
description = "Machine learning algorithms from scratch in pure NumPy"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["numpy>=1.24"]

[project.urls]
Homepage = "https://github.com/YOUR_USERNAME/CMOR-438"
